import 'package:flutter_bloc/flutter_bloc.dart';
import 'state.dart';

class MyCubit extends Cubit<MyState> {
  MyCubit() : super(const MyState());

  // Aquí puedes agregar tus métodos como cargarDatos(), actualizar(), etc.
}
